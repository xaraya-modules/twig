{% if onchange is not null %}
      {% set evt_change = onchange %}
    {% else %}
      {% set evt_change = 'return true;' %}
    {% endif %}

    <input type="text" name="input_type_{{ name }}" id="{{ id }}_input_type" value="{{ input_type }}" hidden="hidden"/>
    {% if input_type == 'dropdown' %}
		{{ include('@property/datetime/includes/datetemplate-input.html.twig') }}
		{{ include('@property/datetime/includes/timetemplate-input.html.twig') }}
    {% else %}
    	<input type="datetime-local" name="{{ name }}" id="{{ id }}" value="{{ value }}"/>
    {% endif %}

    {{ include('@base/includes/dataproperty_additions.html.twig') }}
