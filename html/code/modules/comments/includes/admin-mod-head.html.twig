{# @comments/includes/admin-mod-head.html.twig #}

{% block admin_mod_head %}
<div class="xar-mod-head">
        <span class="xar-mod-title">
            {% if xar_modulevar('comments', 'use_module_alias') %}
                {{ xar_var({scope: "module", module: "comments", name: "module_alias_name"}) }}
            {% else %}
                Comments Administration
            {% endif %}
        </span>
        {{ include('@comments/includes/admin-menu.html.twig') }}
    </div>
{% endblock %}