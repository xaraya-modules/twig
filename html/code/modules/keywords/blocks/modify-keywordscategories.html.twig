{# @keywords/blocks/modify-keywordscategories.html.twig #}

{% block modify_keywordscategories %}
<input type="hidden" value="1" name="refreshrandom" />
  {# <!--
                <div style="clear: both; padding-top: 10px;">
                <span style="float: left; width: 20%; text-align: right;">
                        <span class="help" title="{{ xar_translate('Choose the base category for the related categories.') }}"><label for="catfilter{{ blockid }}">Category</label>:</span>
                </span>

                <span style="float: right; width: 78%; text-align: left;">
                        <select name="cid" id="catfilter{{ blockid }}">
                                <option value="0">All</option>
                                {% for catlistx in categorylist %}
                                        {% if cid and cid == catlistx['cid'] %}
                                                <option value="{{ catlistx['cid'] }}" selected="selected">{{ catlistx['name'] }}</option>
                                        {% else %}
                                                <option value="{{ catlistx['cid'] }}">{{ catlistx['name'] }}</option>
                                        {% endif %}
                                {% endfor %}
                        </select>
                </span>
                </div>
--> #}
                <div style="clear: both; padding-top: 10px;">
                <span style="float: left; width: 20%; text-align: right;">
                        <span class="help" title="{{ xar_translate('How many minutes to wait before updating this block.') }}"><label for="refreshtime{{ blockid }}">Time to refresh block (minutes)</label>:</span>
                </span>
                <span style="float: right; width: 78%; text-align: left;">
                        <input type="text" name="refreshtime" id="refreshtime{{ blockid }}" value="{{ refreshtime }}" size="7" maxlength="6" />
                </span>
                </div>
{% endblock %}