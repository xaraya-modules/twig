{# @publications/blocks/menu-level1.html.twig #}

{% block menu_level1 %}
{# <!-- 
        Menu for top tabs.
        TODO: write up some documentation and include a default stylesheet.
    --> #}

    {% if not max_level %}
        {% set max_level = 0 %}
    {% endif %}
    {% set subdata = {'parent': root_page,'pages': pages,'max_level': max_level} %}
    {{ include('@publications/blocks/menu-level1.html.twig', subdata) }}
{% endblock %}