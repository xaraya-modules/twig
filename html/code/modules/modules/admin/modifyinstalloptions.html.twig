{# @modules/admin/modifyinstalloptions.html.twig #}

{% block modifyinstalloptions %}
{{ include('@modules/includes/admin-mod-head.html.twig') }}
    {# <!--
    <div class="xar-mod-head">
        <span class="xar-mod-title">{{ displayname }} Module Configuration</span>
    </div>
    --> #}
    <div class="xar-mod-body">
        <form action="{{ xar_moduleurl('modules', 'admin', 'updateinstalloptions', {'regid': regid}) }}" method="post" enctype="application/x-www-form-urlencoded">
            {{ include('@' ~ modname ~ '/includes/installoptions.html.twig') }}
            {# <!--        {{ xar_guifunc(modname, "admin", "installoptions", {main: "false"}) }} --> #}
            <fieldset class="xar-form-actions">
                {% set label = 'Save Configuration' %}
                {{- xar_button({type: "submit", label: label, name: "update"}) -}}
                <input type="hidden" name="authid" id="authid" value="{{ authid }}"/>
            </fieldset>
        </form>
    </div>
{% endblock %}