{# @dynamicdata/admin/rename_static_table.html.twig #}

{% block rename_static_table %}
{{ include('@dynamicdata/includes/admin-mod-head.html.twig') }}
    <div class="xar-mod-body">
        <h2>Rename Item {{ table }}</h2>

        <form method="post" action="{{ xar_currenturl() }}">
            <fieldset>
                <legend>
                    Rename this item?
                </legend>
                The table '{{ table }}' to
                <br />
                {{ xar_data_input({type: "textbox", name: "newtable"}) }}
            </fieldset>

            <div class="xar-form-actions">
                <div class="xar-row">
                    <div class="xar-col"></div>
                    <div class="xar-col">
                        <input type="hidden" name="table" id="table" value="{{ table }}"/>
                        <input type="hidden" name="confirm" id="confirm" value="true"/>
                        {% set label = 'Cancel' %}
                        {{- xar_button({type: "cancel", label: label, name: "noconfirm", id: "noconfirm"}) -}}&#160;
                        {% set label = 'Rename' %}
                        {{- xar_button({type: "submit", label: label}) -}}
                    </div>
                </div>
            </div>
        </form>
    </div>
{% endblock %}