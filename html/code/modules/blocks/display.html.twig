{# @blocks/display.html.twig #}

{% block display %}
<div>
        {# <!-- The div is there to ensure that this template in itself has a root tag
           and the block is erm... blocklevel :-)
      --> #}
        {{ xar_block({instance: name}) }}
    </div>
{% endblock %}