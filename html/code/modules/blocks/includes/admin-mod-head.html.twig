{# @blocks/includes/admin-mod-head.html.twig #}

{% block admin_mod_head %}
<div class="xar-mod-head">
        <span class="xar-mod-title">
            {% if xar_modulevar('blocks', 'use_module_alias') %}
                {{ xar_modulevar('blocks', 'module_alias_name') }}
            {% else %}
                Blocks Administration
            {% endif %}
        </span>
        {{ include('@blocks/includes/admin-menu.html.twig') }}
    </div>
{% endblock %}