{# @categories/admin/newhook.html.twig #}

{% block newhook %}
{% set firstline = xar_translate('Select Category') %}
    {% if preview %}
        {# <!-- Note: we might get a single value here instead of an array, if there is only one tree --> #}
        {% set dummy = xar_coremethod('xarVar', 'fetch', ['hookedcategories_categories', 'isset', categories, [], constant('NOT_REQUIRED','xarVar')]) %}
        {% if categories is not iterable %}
            {% set categories = [categories] %}
        {% endif %}
        {{ xar_data_input({type: "categories", name: "hookedcategories", module: module, itemtype: itemtype, firstline: firstline, categories: categories}) }}
    {% else %}
        {{ xar_data_input({type: "categories", name: "hookedcategories", module: module, itemtype: itemtype, firstline: firstline}) }}
    {% endif %}
{% endblock %}