{# @themes/includes/modulescss.html.twig #}

{% block modulescss %}
<h4>Component - {{ component }}</h4>
    <p>Modules CSS tools deal with the styles defined and included by the Xaraya Modules.</p>
    {% if configurable %}
    {% else %}
        <h4>{{ unmanagednote }}</h4>
    {% endif %}
{% endblock %}