{# @themes/includes/admin-mod-head.html.twig #}

{% block admin_mod_head %}
<div class="xar-mod-head">
        <span class="xar-mod-title">
            {% if xar_modvar('themes', 'use_module_alias') %}
                {{ xar_var({scope: "module", module: "themes", name: "module_alias_name"}) }}
            {% else %}
                Themes Administration
            {% endif %}
        </span>
        {{ include('@themes/includes/admin-menu.html.twig') }}
    </div>
{% endblock %}