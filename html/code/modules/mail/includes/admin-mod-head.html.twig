{# @mail/includes/admin-mod-head.html.twig #}

{% block admin_mod_head %}
<div class="xar-mod-head">
        <span class="xar-mod-title">
            {% if xar_modvar('mail', 'use_module_alias') %}
                {{ xar_var({scope: "module", module: "mail", name: "module_alias_name"}) }}
            {% else %}
                Mail Administration
            {% endif %}
        </span>
        {{ include('@mail/includes/admin-menu.html.twig') }}
    </div>
{% endblock %}