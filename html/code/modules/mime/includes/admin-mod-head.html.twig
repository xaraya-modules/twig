{# @mime/includes/admin-mod-head.html.twig #}

{% block admin_mod_head %}
<div class="xar-mod-head">
        <span class="xar-mod-title">
            {% if xar_modulevar('mime', 'use_module_alias') %}
                {{ xar_modulevar('mime', 'module_alias_name') }}
            {% else %}
                Mime Administration
            {% endif %}
        </span>
        {{ include('@dynamicdata/includes/admin-menu.html.twig') }}
    </div>
{% endblock %}